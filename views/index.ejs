<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>Mein cooler Blog</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Willkommen auf meinem Blog!</h1>

    <h2>Neuen Beitrag erstellen</h2>
    <form action="/upload" method="POST">
        <input type="text" name="title" placeholder="Titel" required>
        <br><br>
        <textarea name="content" placeholder="Was möchtest du schreiben?" required></textarea>
        <br><br>
        <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" type="submit">Posten!</button>
    </form>

    <hr>

    <h2>Bisherige Posts</h2>
    <div id="posts-container">
        <% posts.forEach(function(post){ %>
            <div class="post">
                <h3><%= post.title %></h3>
                <p><%= post.content.replace(/\n/g, '<br>') %></p>
            </div>
        <% }); %>
    </div>
</body>
</html>